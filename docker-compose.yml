# For local development
version: "2.2"

services:

  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: infra/flaskweb
    links:
      - redis
    ports:
      - "5000:5000"
      
  redis:
    image: redis
