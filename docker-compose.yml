# For local development
version: "2.2"

services:

  web:
    build: .
    dockerfile: Dockerfile
    image: infra/flaskweb
    links:
      - redis
    ports:
      - "5000:5000"
      
  redis:
    image: redis
